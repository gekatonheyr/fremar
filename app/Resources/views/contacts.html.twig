{% extends 'base.html.twig' %}

{% block body %}
    <div class="container text-center">
            {{ form_start(form) }}
            <h2>Welcome to FREMAR contacts catalog. Please choose the action to perform: </h2><br>
            {{ form_widget(form.getContactsList, {'attr' : {'class':'btn btn-primary'}}) }}
            {{ form_widget(form.addNewContact, {'attr' : {'class':'btn btn-warning'}}) }}
            {{ form_end(form) }}
    </div>

{% endblock %}